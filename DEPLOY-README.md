# راهنمای نصب — آموزش پزشکی در یک نگاه (بک‌اند Cloudflare D1)

## این نسخه چه فرقی با قبل دارد؟

نسخه‌ی قبلی همه‌چیز (کاربران، نمرات، پیشرفت) را فقط در حافظه‌ی محلی مرورگر
(localStorage) نگه می‌داشت — یعنی هر گوشی/مرورگر یک دنیای جدا بود. این
نسخه یک بک‌اند واقعی روی **Cloudflare Pages Functions + D1** دارد:
- هر کاربر از هر گوشی/مرورگری وارد شود، اطلاعاتش همان‌جاست.
- شما به‌عنوان ادمین، از هر دستگاهی همه‌ی کاربران و نتایج را می‌بینید.
- در «خروجی و بازنشانی داده‌ها» می‌توانید داده‌ها را دانلود (CSV)، در
  کلیپ‌بورد کپی، یا کامل پاک کنید.
- رمزهای عبور هش می‌شوند (دیگر متن ساده نیستند).

تمام کد سمت سرور در **یک فایل واحد** جمع شده: `functions/api/[[path]].js`
— این کار عمداً انجام شد تا آپلود از گوشی راحت باشد (به‌جای ۱۹ فایل
پراکنده در پوشه‌های تودرتو، فقط همین یکی).

## فایل‌ها

```
index.html, manifest.json, sw.js
book-part1.js ... book-part4.js            (محتوای کتاب — بدون تغییر)
questions-part1.js ... questions-part5.js  (بانک سوالات کامل — بدون تغییر)
schema.sql                                 (ساختار پایگاه‌داده — یک‌بار اجرا می‌شود)
functions/api/[[path]].js                  (کل بک‌اند در یک فایل)
wrangler.toml                              (اختیاری؛ فقط برای خط فرمان)
```

---

## مرحله ۱ — ساخت دیتابیس D1

1. با گوشی یا کامپیوتر وارد [dash.cloudflare.com](https://dash.cloudflare.com) شوید (ثبت‌نام رایگان).
2. **Workers & Pages → D1 SQL Database → Create**.
3. اسمی بدهید (مثلاً `medical-edu-db`) و بسازید.
4. روی دیتابیس بزنید → تب **Console**.
5. فایل `schema.sql` را با یک اپ یادداشت/ویرایشگر متن باز کنید، همه‌اش را
   کپی و در کادر Console پیست کنید، بعد **Execute**. (فقط یک‌بار لازم است.)

## مرحله ۲ — آپلود فایل‌ها در GitHub (با گوشی)

اول فایل zip را روی گوشی از حالت فشرده خارج (Extract) کنید تا پوشه‌ی
کامل با همه‌ی فایل‌ها را داشته باشید. یک حساب رایگان در
[github.com](https://github.com) بسازید، **New repository** بزنید و یک
اسم بدهید (مثلاً `medical-edu-app`).

از مرورگر گوشی (نه اپ گیت‌هاب) وارد مخزن تازه‌ساخته بشوید. این کار دو
بخش دارد:

**بخش الف — فایل‌های معمولی (۱۵ فایل):**
1. **Add file → Upload files**
2. روی لینک «choose your files» بزنید (نه درگ-اند-دراپ — روی گوشی کار
   نمی‌کند)
3. فایل‌های زیر را با هم انتخاب کنید (اپ فایل گوشی معمولاً اجازه‌ی
   انتخاب چندتایی می‌دهد): `index.html`, `manifest.json`, `sw.js`,
   `schema.sql`, `wrangler.toml`, `book-part1.js` تا `book-part4.js`,
   `questions-part1.js` تا `questions-part5.js`
4. **Commit changes**

**بخش ب — فایل بک‌اند (۱ فایل، توی پوشه):**
چون این فایل داخل یک زیرپوشه (`functions/api/`) قرار می‌گیرد و آپلود
معمولی مسیر پوشه را نگه نمی‌دارد، این یکی را جدا و به روش دیگری اضافه
می‌کنیم:
1. برگردید به صفحه‌ی اصلی مخزن → **Add file → Create new file**
2. توی فیلد اسم فایل (بالای کادر متن)، دقیقاً همین را تایپ کنید:
   `functions/api/[[path]].js`
   (وقتی `/` تایپ می‌کنید، گیت‌هاب خودش پوشه‌ها را می‌سازد)
3. فایل `[[path]].js` که فرستادم را با اپ یادداشت باز کنید، همه‌اش را
   کپی کنید، و در کادر بزرگ زیر اسم فایل پیست کنید
4. پایین صفحه **Commit changes**

**بررسی:** به صفحه‌ی اصلی مخزن برگردید؛ باید یک پوشه‌ی آبی `functions`
ببینید. بروید داخلش → `api` → باید فایلی به اسم `[[path]].js` آنجا باشد.

## مرحله ۳ — ساخت سایت در Cloudflare Pages

1. در Cloudflare: **Workers & Pages → Create → Pages → Connect to Git**
2. گیت‌هابتان را وصل کنید، مخزن مرحله‌ی ۲ را انتخاب کنید
3. **Build command**: خالی بگذارید. **Build output directory**: `/`
4. **Save and Deploy**

## مرحله ۴ — وصل‌کردن دیتابیس به سایت

1. بعد از ساخت پروژه: **Settings → Bindings → Add binding → D1 database**
2. **Variable name**: دقیقاً `DB` (حروف بزرگ)
3. دیتابیس مرحله‌ی ۱ را انتخاب کنید → Save
4. تب **Deployments** → آخرین دیپلوی → **Retry deployment**

## مرحله ۵ — تست

1. آدرس سایت چیزی شبیه `medical-edu-app.pages.dev` است
2. با `admin` / `admin` وارد شوید → از تنظیمات رمز را عوض کنید
3. یک حساب تستی بسازید، یک فصل و کوییز را امتحان کنید
4. در پنل مدیریت → مدیریت کاربران، همان حساب تستی را ببینید

---

## نکته برای انتقال بعدی به چابکان

فرانت‌اند فقط با آدرس‌های `/api/...` صحبت می‌کند و به Cloudflare وابسته
نیست. وقتی خواستید به سرور چابکان منتقل کنید، کافی است منطق داخل همین
یک فایل (`[[path]].js`) را در یک برنامه‌ی Node.js با همان مسیرها
بازنویسی کنید و پایگاه‌داده را به MySQL/PostgreSQL ببرید؛ `schema.sql`
تقریباً همان‌طور با تغییرات جزئی نحوی قابل استفاده است.

## محدودیت شناخته‌شده

«مدیریت محتوا» و «مدیریت سوالات» در پنل ادمین فقط در حافظه‌ی موقت مرورگر
است و با رفرش از بین می‌رود — چون قرار شد فایل‌های کتاب و سوالات تغییر
نکنند، این بخش دست‌نخورده ماند.
